function run() {
    //needs to search for "net loss" and "net income" and see which has more results
    //also price range first instance of "between $number and $number"
}

$(window).on("load", function () {
    try {
        setTimeout(run, 500);
    }
    catch (error) {
        setTimeout(run, 500);
    }
});